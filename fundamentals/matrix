matrix=[]
for i in range(3):
    values=[]
    for j in range(3):
        nums=float(input(f"Enter num in row: {i+1}, Enter num in col: {j+1}"))
        values.append(nums)
    matrix.append(values)

print("matrix entered: ")
for mat in matrix:
    print(mat)